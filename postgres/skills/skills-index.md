# Skills

这里用于放可复用的阅读、调试和文档生成 skill。

建议每个 skill 独立一个目录，目录里至少包含：

- `SKILL.md`: skill 的目标、适用场景和执行步骤。
- `references.md`: 关键源码入口、外部资料和术语表。
- `examples/`: 可选，放实际使用样例。

后续可以把 `replay/` 专题里的源码阅读路径提炼成一个 `wal-replay-reading` skill。

